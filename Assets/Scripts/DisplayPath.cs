using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using SimpleFileBrowser;

public class DisplayPath : MonoBehaviour {

	// Use this for initialization
	void Start () {
		/*GameObject go = GameObject.Find("PointCloudGenerator");
		if (go == null)
		{
			Debug.LogError("Failed to detect Tirgger");
			this.enabled = false;
			return;
		}

		PointCloudGenerator ic = go.GetComponent<PointCloudGenerator>();

		//path = ic.pathValue;

		GetComponent<Text>().text = "path : " + ic.path;*/
	}
	
	// Update is called once per frame
	void Update () {
		

	}
}
